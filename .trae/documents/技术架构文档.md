## 1. 架构设计

```mermaid
graph TD
    subgraph "前端表现层 (React 18)"
        A["页面组件 (Pages)"]
        B["UI组件 (Components)"]
        C["状态管理 (Zustand)"]
        D["路由管理 (React Router)"]
    end
    
    subgraph "业务逻辑层"
        E["日期计算工具"]
        F["国家免疫规划数据"]
        G["儿保体检标准数据"]
        H["提醒生成逻辑"]
        I["导出格式化引擎"]
    end
    
    subgraph "数据持久层"
        J["LocalStorage 存储"]
        K["类型定义 (TypeScript)"]
    end
    
    subgraph "外部服务"
        L["浏览器 Notification API"]
        M["浏览器 Print API"]
    end
    
    A --> B
    A --> D
    B --> C
    C --> J
    E --> F
    E --> G
    H --> E
    H --> L
    I --> M
    F --> K
    G --> K
```

## 2. 技术描述
- **前端框架**：React@18 + TypeScript@5
- **构建工具**：Vite@5
- **样式方案**：TailwindCSS@3 + CSS Variables
- **路由管理**：React Router DOM@6
- **状态管理**：Zustand@4（轻量、简洁、TS友好）
- **图标库**：Lucide React（线性图标，现代风格）
- **数据存储**：浏览器 LocalStorage（纯前端，无需后端）
- **初始化工具**：vite-init（react-ts 模板）

## 3. 路由定义
| 路由路径 | 页面组件 | 用途 |
|----------|----------|------|
| / | Dashboard | 首页仪表盘，概览信息和快捷入口 |
| /child-info | ChildInfo | 孩子信息录入与编辑 |
| /vaccine-schedule | VaccineSchedule | 疫苗接种时间表 |
| /checkup-schedule | CheckupSchedule | 儿保体检时间表 |
| /reminders | Reminders | 提醒中心 |
| /records | Records | 接种/体检记录管理 |
| /export | ExportPrint | 导出打印中心 |

## 4. 数据模型

### 4.1 数据模型ER图

```mermaid
erDiagram
    CHILD ||--o{ VACCINE_SCHEDULE : has
    CHILD ||--o{ CHECKUP_SCHEDULE : has
    CHILD ||--o{ VACCINE_RECORD : has
    CHILD ||--o{ CHECKUP_RECORD : has
    CHILD ||--o{ REMINDER : has
    VACCINE_SCHEDULE ||--o{ VACCINE_RECORD : "completed by"
    CHECKUP_SCHEDULE ||--o{ CHECKUP_RECORD : "completed by"
    
    CHILD {
        string id PK "唯一ID"
        string name "孩子姓名"
        string gender "性别：男/女"
        date birthDate "出生日期"
        string avatar "头像(可选)"
        datetime createdAt "创建时间"
        datetime updatedAt "更新时间"
    }
    
    VACCINE_SCHEDULE {
        string id PK "唯一ID"
        string childId FK "孩子ID"
        string vaccineCode "疫苗编码"
        string vaccineName "疫苗名称"
        int doseNumber "剂次"
        string preventDisease "预防疾病"
        int monthAge "推荐月龄"
        date plannedDate "计划接种日期"
        string status "状态：待接种/已接种/已推迟/已错过"
        string route "接种途径"
        string site "接种部位"
        string contraindications "禁忌症"
        string notes "备注"
    }
    
    VACCINE_RECORD {
        string id PK "唯一ID"
        string childId FK "孩子ID"
        string scheduleId FK "对应计划ID"
        string vaccineName "疫苗名称"
        string manufacturer "生产厂家"
        string batchNumber "疫苗批号"
        date vaccinationDate "接种日期"
        string site "接种部位"
        string doctor "接种医生(可选)"
        string reaction "当日反应"
        string reactionSeverity "反应程度：无/轻微/中度/严重"
        string notes "备注"
        datetime createdAt "创建时间"
    }
    
    CHECKUP_SCHEDULE {
        string id PK "唯一ID"
        string childId FK "孩子ID"
        int monthAge "月龄"
        date plannedDate "计划体检日期"
        string status "状态：待体检/已体检/已错过"
        string items "体检项目列表(JSON)"
        string standards "发育标准参考(JSON)"
        string notes "注意事项"
    }
    
    CHECKUP_RECORD {
        string id PK "唯一ID"
        string childId FK "孩子ID"
        string scheduleId FK "对应计划ID"
        int monthAge "月龄"
        date checkupDate "体检日期"
        float weight "体重(kg)"
        float height "身高(cm)"
        float headCircumference "头围(cm)"
        string bmi "BMI评价"
        string development "发育评估"
        string itemsResult "体检项目结果(JSON)"
        string doctorAdvice "医生建议"
        string notes "备注"
        datetime createdAt "创建时间"
    }
    
    REMINDER {
        string id PK "唯一ID"
        string childId FK "孩子ID"
        string type "类型：vaccine/checkup"
        string relatedId "关联的计划ID"
        string title "提醒标题"
        date dueDate "到期日期"
        date remindDate "提醒日期(提前3天)"
        string status "状态：待提醒/已提醒/已完成"
        int daysBefore "提前天数(默认3)"
        datetime notifiedAt "通知时间"
    }
```

### 4.2 国家免疫规划疫苗数据结构

```typescript
// 国家免疫规划疫苗基础数据（参考中国疾控中心最新标准）
interface VaccineDefinition {
  code: string;          // 疫苗编码，如 'BCG'
  name: string;          // 疫苗全称
  shortName: string;     // 简称
  preventDisease: string;// 预防疾病
  doses: DoseInfo[];     // 各剂次信息
  route: string;         // 接种途径
  contraindications: string[]; // 禁忌症
  commonReactions: string[];   // 常见反应
  category: '一类' | '二类';  // 疫苗类别
}

interface DoseInfo {
  doseNumber: number;    // 第几剂
  monthAgeMin: number;   // 最小月龄
  monthAgeMax: number;   // 最大月龄（窗口期）
  recommendedMonthAge: number; // 推荐月龄
  site: string;          // 推荐接种部位
  intervalAfterPrevious?: number; // 与上一剂间隔天数
}

// 儿保体检标准数据
interface CheckupDefinition {
  monthAge: number;
  items: CheckupItem[];
  standards: DevelopmentalStandards;
  notes: string[];
}

interface CheckupItem {
  name: string;
  category: '体格测量' | '全身检查' | '发育评估' | '辅助检查' | '其他';
  description: string;
}

interface DevelopmentalStandards {
  weight: { boy: { min: number; max: number; median: number }; girl: { min: number; max: number; median: number } };
  height: { boy: { min: number; max: number; median: number }; girl: { min: number; max: number; median: number } };
  milestones: string[]; // 发育里程碑
}
```

## 5. 工具函数核心逻辑

### 5.1 日期计算工具
- `calculateMonthAge(birthDate: Date, targetDate?: Date): number` - 计算月龄
- `addMonths(date: Date, months: number): Date` - 日期加减月份
- `formatDate(date: Date, format?: string): string` - 日期格式化
- `getDaysBetween(date1: Date, date2: Date): number` - 计算天数差
- `generateVaccineDates(birthDate: Date): VaccineSchedule[]` - 根据出生日期生成接种计划

### 5.2 提醒逻辑
- 应用启动时扫描所有未完成的计划
- 计算距今日 ≤ 提前天数(默认3天)且未提醒的事项
- 调用浏览器 Notification API 推送桌面通知
- 更新提醒状态为"已提醒"

### 5.3 导出格式化
- 支持三种模板：入托版、入学版、完整版
- 生成 HTML 表格（适合打印）
- 调用 window.print() 打印
- 预留导出 PDF 接口（可集成 html2pdf.js）
